# LLM 서빙 기법이 VLA 서빙에서 되는지 안 되는지 정리

> 작성: 2026-08 | 근거: RESULTS_A100.md (A100 80GB ×2, 컴파일 경로), GB10 측정, 시뮬레이션 (sim_sched2/3.py)
> 대상 모델: π0.5 (PaliGemma 백본 + MoT action expert, flow matching 10스텝, replan_steps=5 @ 20Hz → 250ms 주기 데드라인)

## 요약

LLM 서빙 최적화는 대부분 세 가지 문제를 푸는 기법이다: **메모리 부족, 출력 길이가 제각각인 것, 긴 컨텍스트.**
VLA 서빙에는 이 세 문제가 없다. 대신 **연산 시간, 호스트 오버헤드, 데드라인 처리**가 문제다.
그래서 (1) LLM 스택의 큰 부분이 그대로 안 먹히고, (2) 반대로 LLM에서는 못 쓰던 기법들
(실시간 스케줄링 이론, 프레임 간 유사성 활용)을 쓸 수 있게 된다.

판정의 근거가 되는 사실들 (측정값 + 모델 구조):

| 사실 | 출처 |
|---|---|
| prefix(이미지+텍스트)에 양방향 어텐션 — 모든 토큰이 서로 참조 | PaliGemma/π0 구조 |
| 토큰 순서가 이미지 먼저, 텍스트 나중 | π0 입력 순서 |
| MoT: prefill(VLM expert)과 denoise(action expert)가 다른 가중치 사용 | π0.5 구조 |
| 요청 비용이 일정함: prefill 1회 + 10스텝, p99−mean ≈ 1ms | A100 측정 |
| 출력 길이가 전 요청 동일 (분산 0) | 구조 |
| 메모리 여유 많음: B=16에 9.24 GiB | GB10 측정 |
| prefill은 B=1에서 이미 SM 포화 (B=1→2가 1.95배) | A100 측정 |
| 어텐션은 GPU 시간의 0.2% (26/11,377ms) | GB10 nsys |
| 호스트 갭 25~30% (GPU-busy 70.7%, 런치 5천/req, 느린 CPU) | A100 측정 |
| 연속된 요청은 같은 장면을 250ms 간격으로 찍은 것 | 워크로드 성질 |
| 클라이언트(로봇 제어 루프)가 우리 코드임 — 요청 타이밍을 우리가 정할 수 있음 | 배치 환경 |
| 늦게 도착한 액션은 그냥 버려짐 (다음 청크가 덮어씀) | 제어 구조 (cf. RTC) |

판정 기호: **✕** 구조적으로 안 됨 / **△** 되긴 하는데 지금 워크로드에선 이득 없음(조건 바뀌면 유효) / **○** 될 것 같은데 아직 안 재봄 / **◎** 바로 적용 가능 (측정 근거 있음)

---

## 표 1. LLM → VLA: 뭐가 안 되나

| # | Technique (vLLM V1 / 논문) | 판정 | 이유 | 다시 유효해지는 조건 |
|---|---|---|---|---|
| 1 | Continuous batching | △ | CB가 필요한 이유는 출력 길이가 제각각이라서인데, VLA는 전 요청이 prefill 1회+10스텝으로 동일 → 그냥 배치와 다를 게 없음. denoise 링 실측에서도 짐 (§4, 미스 100%) | 로봇마다 스텝 수를 다르게 주는 순간 (adaptive steps) |
| 2 | Chunked prefill — 토큰 축 (Sarathi) | ✕ | 양방향 어텐션이라 토큰을 쪼개면 앞 토큰의 KV가 틀려짐 | 없음 (causal 백본 VLA면 가능) |
| 2′ | Chunked prefill — 레이어 축 | △ | 결과는 정확히 같게 나오지만, 로봇들 조건이 다 같아서 쪼개봤자 한쪽 살리면 다른 쪽이 죽음 (시뮬: 미스 개선 없음, 오버헤드만 +0.8ms/청크) | 여유가 많은 다른 작업(모니터 등)을 쪼갤 때만 |
| 3 | Automatic prefix caching | ✕ | 두 가지 이유로 안 됨: (a) 이미지 토큰이 앞이라 공유되는 텍스트가 prefix 위치에 없음, (b) 양방향 어텐션이라 같은 텍스트 토큰이라도 KV가 그 프레임 이미지에 따라 달라짐 → 프레임이 바뀌면 재사용 불가 | 없음. 캐시할 수 있는 KV라는 게 애초에 없음 |
| 4 | Cascade attention (배치 내 공유 prefix) | ✕ | #3과 같은 이유 | 없음 |
| 5 | PagedAttention | △ | 부족한 게 메모리가 아님. 컨텍스트 길이도 전부 같아서 단편화가 안 생김 | long-context VLA (VLA-Perf: 1K스텝에 KV 13GB) |
| 6 | KV offloading (LMCache connector) | △ | 로봇당 KV ~150MB, 다 합쳐도 1GiB 미만 — 내릴 게 없음 | #5와 같은 조건 |
| 7 | Preemption (RECOMPUTE/SWAP) | △ | vLLM의 preemption은 메모리가 모자랄 때 쓰는 건데, 우리한테 필요한 건 데드라인 때문에 끼어드는 것 — 목적이 다름. 그리고 로봇들 여유가 다 같으면 끼어들어봤자 본전 (링·chunking·스트림 3개 다 실측으로 기각) | 여유가 다른 작업이 섞일 때 (colocation, SLO 섞인 플릿) |
| 8 | P-D disaggregation | ○ | MoT라서 가중치가 자연스럽게 갈라짐 (GPU0=VLM expert, GPU1=action expert, 양쪽에 다 실을 필요 없음). KV 전송도 ~150MB ≈ PCIe 6ms라 예산 안에 들어옴 | 검증 필요: prefill:denoise = 163:53 불균형을 어떻게 나눌지, 그리고 그냥 서버 2개 띄우는 것(DP)보다 나은지 |
| 9 | Speculative decoding (EAGLE/MTP) | ✕ | AR decode가 없음 — flow matching은 스텝당 forward 한 번이라 검증할 토큰 시퀀스가 없음 | discrete-token VLA(OpenVLA류)에는 그대로 적용됨. flow에서 비슷한 역할을 하는 건 스텝 줄이기 (표 2 #3) |
| 10 | CUDA graph 커버리지 확대 (-O2 FULL_AND_PIECEWISE) | ◎ | 호스트 갭 25~30%, 요청당 런치 5천 개 측정됨. denoise 10스텝을 그래프 하나로 캡처하는 게 제일 싸고 효과 큼 | — (바로) |
| 11 | Async scheduling / zero-overhead scheduler (V1) | ◎ | vLLM V1이 이걸 만든 이유가 우리 증상이랑 똑같음 (스케줄링·전처리가 GPU를 놀림). obs 조립 6ms/배치 + 6.6ms/req를 실행이랑 겹치면 됨 | — (바로) |
| 12 | Quantization (FP8/FP4/INT8) | ○ | prefill이 B=1부터 compute-bound라 연산량을 줄이면 latency가 바로 줄어듦. 참고로 A100엔 FP8 유닛이 없고 GB10(Blackwell)엔 있어서 edge 쪽이 오히려 이득이 큼 | 품질 영향을 성공률로 재봐야 함 (스텝-성공률 스윕이랑 같은 하네스) |
| 13 | Tensor parallelism | △ | TP=2로 prefill latency는 줄겠지만 3B 모델에 PCIe 통신이면 통신비로 다 까먹을 수 있음. 그냥 서버 2개(DP, 확실한 2배)가 만만치 않은 상대 | 벤치 한 번 돌려보면 결론 남 |
| 14 | Multi-LoRA (S-LoRA) | ○ | 로봇마다 태스크가 다르면 어댑터만 다른 요청들을 한 배치에 태울 수 있음. π0.5가 배포처마다 fine-tune하는 모델이라 시나리오도 자연스러움 | 안 재봄 |
| 15 | Priority scheduling | △ | vLLM에 priority 파라미터는 있는데 주기, 데드라인, admission control 개념이 없음 — 이 빈 자리가 우리가 뭔가 할 수 있는 곳 | 우리가 만들 부분 |
| 16 | 어텐션 커널 (FlashAttention 3 등) | ✕ | 어텐션이 GPU 시간의 0.2%밖에 안 됨 — 최적화할 대상이 아님. 컨텍스트가 짧아서 GEMM이 시간을 다 씀 | long-context 가면 다시 볼 것 |

**정리:** 안 되는 것들(1–6, 9, 16)은 전부 LLM의 세 문제(메모리, 길이 분산, 긴 컨텍스트)를 푸는 기법이고, VLA엔 그 문제가 없다.
되는 것들(10–12)은 우리의 진짜 문제(연산 시간, 호스트 오버헤드)를 푸는 기법.
그리고 15번처럼 아예 비어 있는 곳이 있다 — vLLM엔 "빨리, 많이"는 있어도 "언제까지"가 없다.

---

## 표 2. 반대 방향: LLM에선 못 썼는데 VLA에선 쓸 수 있는 것

LLM 서빙이 어려운 근본 이유 네 가지와, VLA에서는 어떻게 뒤집히는지:

| LLM에서는 | VLA에서는 |
|---|---|
| C1. 출력 길이를 몰라서 요청 비용을 예측 못 함 | 비용이 일정함 (prefill + K스텝, p99−mean ≈ 1ms) |
| C2. 요청이 언제 올지 모름 (클라이언트가 인간) | 클라이언트가 우리 코드 — 요청 타이밍을 정할 수 있음 |
| C3. 품질을 깎아서 시간을 벌 방법이 없음 (토큰을 빼면 문장이 망가짐) | 스텝 수, prefix 신선도라는 품질 조절 손잡이가 있음 |
| C4. 요청끼리 아무 관련 없음 | 연속 요청이 같은 장면의 250ms 차이 샘플 |

| # | 기법/이론 | LLM에서 왜 못 썼나 | VLA에서 왜 되나 | 상태 |
|---|---|---|---|---|
| 1 | 고전 실시간 스케줄링 이론 (WCET, utilization bound, schedulability test, admission control) | C1. 비용을 모르니 이론의 전제가 무너짐. FastServe가 MLFQ로 SRPT를 흉내내야 했던 이유. "GPU에 몇 대까지 되나"를 보장 못 함 | 비용이 일정해서 WCET가 그냥 주어짐 → "로봇 N대까지 데드라인 보장"을 Liu & Layland 방식으로 계산·증명 가능 | 아무도 안 함. ROSA는 오프라인 프로파일링으로 때웠지 이론을 안 씀 |
| 2 | 요청 타이밍을 서버가 정하기 (TDMA처럼 위상 슬롯 배정) | C2. 사람한테 "질문은 250ms 정각에만 해주세요"라고 할 수 없음 | 서버가 로봇마다 요청 시각 슬롯을 정해주면 됨 → barrier batching이 우연히 만들던 동기화를 설계로 만듦. 더 가면 replan_steps 협상으로 주기 자체도 조절 가능 | 아무도 안 함. 서버-클라이언트를 같이 설계한 사례 없음 |
| 3 | Imprecise computation — 일은 필수 부분 + 선택 부분으로 나누고, 급하면 선택 부분을 버리는 것 (Liu et al., 1980년대 실시간 이론) | C3. 모든 토큰이 필수라 버릴 게 없음 | flow matching이 정확히 이 구조: prefill = 필수, denoise 각 스텝 = 선택적 개선. 과부하면 뒷 스텝을 버리면 됨 | 시뮬로 예측 완료 (adaptive≥3: N=6 미스 48→0%, N=7 91→18%). 스텝-성공률 스윕만 남음 |
| 4 | 바뀐 부분만 다시 계산하기 (비디오 코덱의 I/P-frame 방식) | C4. 이번 요청과 다음 요청 사이에 재사용할 게 없음 | full prefill = I-frame, 바뀐 패치만 갱신 = P-frame. prefill 33ms를 "장면이 얼마나 바뀌었나"에 비례하는 비용으로 바꿈 | 비전 쪽 선행 연구는 있음 (Eventful Transformers 등), 서빙 시스템으로 만든 건 없음. 양방향 어텐션이랑 맞는지가 관건 |
| 5 | 요청이 오기 전에 미리 계산해두기 (speculative prefill) | C1+C4. 다음 요청 내용을 모르니 미리 할 게 없음 | 다음 요청이 언제 올지(t+250ms)와 뭐가 올지(지금 프레임과 거의 같음) 둘 다 앎 → 도착 전에 prefill 해두고 도착하면 denoise만 → 체감 latency가 denoise만큼으로 줄어듦 | 아무도 안 함. #4의 신선도-성공률 데이터가 여기서도 근거로 쓰임 |
| 6 | 데드라인 놓친 요청 그냥 버리기 | 응답을 안 주면 그게 서비스 장애 | 늦은 액션은 어차피 다음 청크가 덮어써서 소용없음 → 놓친 사이클은 버리는 게 맞음. RTC가 정책 쪽에서 보인 걸 서빙 쪽에 적용하는 셈 | 절반은 됨 (RTC). 스케줄러에 넣은 사람은 없음 |
| 7 | forward 전체를 커널 몇 개로 합치기 (megakernel / persistent kernel) | seq 길이, 배치가 요청마다 달라서 전체를 고정 그래프로 못 만듦 | shape 종류가 몇 개 안 됨: seq ~1000 고정, 스텝 10 고정, 배치 ≤8 → 컴파일 시점에 계산 전체가 정해짐. 런치 5천 개 → 수십 개 | VLA에선 아무도 안 함. 호스트 갭 25~30%가 바로 타깃. 표 1 #10에서 이어지는 방향 |
| 8 | 전력/클럭 조절 (DVFS) | C1. 여유가 얼마인지 모르니 클럭을 낮출 근거가 없음 | 주기와 비용을 아니까 여유가 계산됨 → 클럭을 낮출지 빨리 끝내고 쉴지를 이론대로 결정 가능 | 아무도 안 함. 우선순위는 낮음. edge 박스(GB10 57~66W 측정)에서나 의미 |

**묶어서 보면 두 갈래다:**
- **실시간 스케줄링 이론을 쓸 수 있게 됨** (#1, 2, 3, 8): LLM이 비용을 몰라서(C1) 못 쓰던 이론들이 VLA에선 그대로 적용됨.
- **프레임 간 유사성 활용** (#4, 5, 6): 요청들이 따로따로가 아니라 하나의 장면을 연속으로 찍은 샘플이라는 점(C4)을 쓰는 것.
- #7은 shape이 고정이라는 점에서 나오는 컴파일 쪽 보너스.

**서로 얽혀 있음:** #5(미리 계산)는 #3의 품질 손잡이가 있어야 안전하고, #3에서 스텝을 버리는 결정은 #1의 schedulability 계산 위에서 내려야 하고, #2의 타이밍 배정은 #1 이론 안의 변수다. 한 문장으로 하면:

> LLM 서빙은 모르는 것(비용, 도착)을 상대하는 시스템이고, VLA 서빙은 아는 것 위에서 도는 시스템이다.
> 남는 불확실성은 시간이 아니라 품질 쪽에 있다 — 그리고 품질은 측정하면 된다.

---

## 주의할 점

1. **표 2 #1의 "비용이 일정함"은 지금처럼 로봇들이 다 같은 조건일 때 얘기.** colocation, long-context, multi-LoRA가 들어오면 비용이 다시 제각각이 되고 LLM 쪽 문제가 일부 돌아온다. 어디까지 버티는지 자체가 연구 질문이 될 수 있다.
2. **표 2의 #3, 4, 5는 전부 품질을 깎는 기법** → 어느 길로 가든 "스텝 수 / prefix 신선도 vs LIBERO 성공률" 측정을 먼저 해야 한다. 이 스윕이 표 절반의 선행 조건.
3. 표 1 판정은 π0.5(양방향 prefix, MoT, flow matching) 기준. causal 백본이나 discrete-token VLA에서는 #2, #9가 뒤집힌다 — 모델 계열별로 따로 봐야 한다.
4. 시뮬 수치는 실측 대비 ±10%p 정도 어긋난다 (batch 미스율 31 vs 19%). 절대값보다 정책 간 순위로 읽을 것.

## 다음 실험

| 우선순위 | 실험 | 뭘 확인하나 |
|---|---|---|
| 1 | 스텝 수 vs LIBERO 성공률 스윕 (+ prefix 신선도 축 추가) | 표 2 #3, 4, 5의 공통 선행 조건 |
| 2 | denoise 10스텝 단일 CUDA graph 캡처 | 표 1 #10, 표 2 #7의 첫 단계 |
| 3 | 모니터 VLM 실물 colocation (FIFO / 절대우선 / slack 슬라이싱 / MPS SM 분할) | 표 1 #7, 2′이 살아나는 조건 |
| 4 | MoT 분할 P-D disagg vs 서버 2개(DP) | 표 1 #8 |
| 5 | GB10 FP8 prefill | 표 1 #12 |
